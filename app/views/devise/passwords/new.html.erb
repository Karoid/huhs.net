<div class="container box">
  <center>
<h2>비밀번호를 잊어버렸습니까?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    임원진 혹은 사이트 관리자에게 문의하시면 비밀번호를 초기화시켜드립니다!
  </div>

<% end %>

<%= render "devise/shared/links" %>
</center></div>
